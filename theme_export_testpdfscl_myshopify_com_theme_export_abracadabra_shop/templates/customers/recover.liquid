
<div class="max-w-md mx-auto mt-20 p-6 bg-white rounded shadow">
  <h1 class="text-2xl font-bold mb-4">Восстановление пароля</h1>

  {% if form.errors %}
    <div class="mb-4 text-red-600">
      {% for field in form.errors %}
        <p>{{ field | capitalize }}: {{ form.errors[field] }}</p>
      {% endfor %}
    </div>
  {% endif %}

  {% if form.posted_successfully? %}
    <p class="text-green-600 mb-4">
      Если email существует в нашей базе, ссылка для восстановления пароля отправлена.
    </p>
  {% endif %}

  <form method="post" action="{{ routes.account_recover_password_url }}">
    <input type="hidden" name="form_type" value="recover_customer_password">
    <input type="hidden" name="utf8" value="✓">

    <div class="mb-4">
      <label for="email" class="block mb-1 font-medium">Email</label>
      <input
        type="email"
        name="email"
        id="email"
        class="w-full border rounded px-3 py-2 focus:outline-none focus:ring focus:border-blue-500"
        required
        value="{{ form.email }}"
      >
    </div>

    <button type="submit"
      class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700 focus:outline-none focus:ring">
      Отправить ссылку для восстановления
    </button>
  </form>

  <p class="mt-4 text-sm text-gray-600">
    <a href="{{ routes.account_login_url }}" class="underline text-blue-600">Вернуться к входу</a>
  </p>
</div>
